import Navbar from "../navbar/Navbar";
import Hero from "../hero/Hero";

const Home = () => {
  return (
    <section id="home">
      <Navbar />
      <Hero />
    </section>
  );
};

export default Home;
